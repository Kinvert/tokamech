- [ ] snake_filter_unsafe_action, we want to avoid things like this. The goal isn't to tell the snake how to do well. The goal of this repo is to make robust ways to train the snake to learn such simple things. The snake should **LEARN** to not do unsafe actions. It should be allowed to do these unsafe actions, and ideally when it's new and dumb and thrashing about, it makes these mistakes, gets a negative reward or something like that, and it learns to stop on it's own!!!
- [ ] if (!env)  no no no we don't do fake safetyism here. This isn't made for retards that will pass in a sandwich instead of the env. We don't create bloat with things like "if step < 0" or "if time < 0" and dumb shit like that. We don't make sure arrays have positive rather than negative length. We just make sure our code is clean and that we're doing sane things in the code itself rather than these sorts of silly checks. This isn't airliner avionics code that needs infinite layers of safety. Add a brief note to SPEC about this. No safetyism bloat. Here is another example if (suggested_action >= SNAKE_ACTION_COUNT). 
- [ ] We should not be suggesting actions programatically!!!! These should be decided by a model or policy or neural net or token predictor or whatever, and then it tries that action, it's a good or bad action, and it learns through rewards and penalties. This is to be a learning tool like RL, like an alternative to standard RL. This isn't meant to just programatically get the snake game to work, that's retarded. This repo is meant to use multiple next token type shit to choose actions, and to TRAIN these to do well. I don't want to see in the env.c or env.h or anything like that, parts of code putting guardrails on the actions besides like clipping them -1 to 1 for example.
- [ ] snake_filter_best_score_action no no no we should not be telling the snake what to do!!!! We are supposed to use next token prediction or whatever. What are we even doing here??? So far the whole theme I see is you telling the snake what to do. No. We create a framework where the snake can learn what to do!!! If we need a "ground truth" then we can write PufferLib envs and use that to generate good and bad actions at first, we make a huge list of that or some shit, then we like tokenize that, then we train the thing to predict next tokens. Something like that. Not like this where we just tell the "agent" what to do. Is this even an agent? It's like we just programatically tell it what to do, that's bullshit.
