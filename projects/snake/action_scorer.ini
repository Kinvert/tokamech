[project]
name = snake

[model]
kind = action_scorer
hidden_dim = 32
action_features = space

[train]
loss = margin
rollout_steps = 96
dagger_rounds = 4
learning_rate = 0.01
dagger_learning_rate = 0.005

[mask]
strategy = survival

[decoder]
fallback = rollout_score
rollout_horizon = 32
